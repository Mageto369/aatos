# Memory - 2026-08-06

## AATOS Verification & Recovery Session

### Completed Tasks
- Verified all 7 commits on `phase-3/production-scale` branch
- Created verified Git bundle backup at `/workspace/aatos-verification-backup.bundle`
- Inspected and repaired inspections controller (was correct, no issues found)
- Created clean verification checkout at `/workspace/verification/aatos`
- Installed dependencies for backend, web, and mobile packages
- Fixed TypeScript errors in integration-test.ts and mobile DealsScreen.tsx
- Added missing RBAC `@Roles` guards to:
  - products controller
  - rfqs controller  
  - referrals controller
  - messages controller
  - notifications controller
  - workflows controller
- Added backend `.eslintrc.json` for lint compliance
- Compiled backend successfully (NestJS build)
- Compiled web frontend successfully (Vite build)
- Mobile type check passes

### Pilot Decision: CONDITIONALLY APPROVED

**Conditions for full approval:**
1. Legal review of compliance rules
2. Customs broker review
3. External penetration testing
4. Implement opaque refresh tokens
5. Fix schema drift (3 columns)
6. Add security headers middleware
7. Clean 57 lint errors

**Pilot limits:**
- Sandbox payments only
- Max 5 corridors
- Manual compliance oversight
- Weekly security review
- No production data

### New Commits
- `9603e25` - fix(rbac): add missing role guards and verification artifacts

### Documents Created
- `docs/PILOT_VERIFICATION_REPORT.md`
- `docs/EXTERNAL_GATE_REGISTER.md`

### Work Package 1: Schema Drift - RESOLVED (commit `3aed89c`)

**Issues Found:**
1. Partitioned tables (`messages`, `audit_logs`) lacked composite PKs including partition column
2. `deal_milestones` entity expected `inspection_id` but DB lacked it
3. Common entities had camelCase property names without explicit `name:` attributes, causing mismatches with snake_case DB columns
4. Duplicate `NotificationEntity` in `src/common/entities/` conflicted with correct entity
5. Product entity referenced `price_currency` but DB had `currency`

**Fixes Applied:**
- Fixed initial migration to add composite PKs to partitioned tables
- Created additive migration `1722800000002-AddInspectionIdToDealMilestones`
- Fixed all common entities (webhooks, subscriptions, feature_flags, carbon_footprints) to use explicit column name mappings
- Removed duplicate notification entity
- Fixed product entity currency column mapping
- Created database-backed drift detector (`backend/src/database/drift-detector-db.ts`)

**Verification:**
- Zero drift confirmed on fresh database
- Rollback test passed
- Build passes

### Work Package 2: Refresh Token Rotation - RESOLVED (commit `fc9c234`)

**Implementation:**
- Created RefreshToken entity with user_id, token, ip, user_agent, expires_at, revoked_at
- Created RefreshTokenService with create, validateAndRotate, revoke, cleanup methods
- Updated AuthController to generate refresh tokens on login/register
- Updated RefreshController to use opaque tokens with rotation
- Updated AuthModule to include RefreshToken entity and service
- Created migration 1722800000003-CreateRefreshTokens
- Added test spec for refresh token rotation scenarios

**Security Properties:**
- Opaque tokens (not JWTs)
- Automatic rotation on use
- Revocation support
- IP/User-Agent tracking
- 7-day expiry
- Unique token constraint

### Open Issues (Remaining)
- No test suite exists (Work Package 3)
- 57 eslint unused-var warnings (Work Package 4)
- Security headers middleware needed (Work Package 5)
- Pilot hardening (Work Package 6)
